## настройка backend

## настройка .env
1. Задите в директории backend
2. Отредактируйте файл .env, указав необходимые переменные окружения, такие как:
```PORT=5000
DB_HOST=localhost
DB_PORT=3306
DB_USER= your_username
DB_PASSWORD=your_password
DB_NAME=your_database_name
JWT_SECRET=your_jwt_secret
```

1. Установить зависимости
```bash
npm install
```
2. Запустить сервер
```bash
npm start
```
3. Сервер будет доступен по адресу http://localhost:3000

## настройка frontend
1. Задите в директории frontend
установить зависимости
```bash
npm install
```
2. Запустить приложение
```bash
npm start
```